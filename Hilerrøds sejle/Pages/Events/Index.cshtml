@page
@model Hilerrøds_sejle.Pages.Events.IndexModel
@{
}

<h2>Events</h2>
<ul>
    @foreach (var e in Model.Events)
    {
        <li>
            <strong>@e.Navn</strong> – @e.Tidspunkt.ToString("dd.MM.yyyy HH:mm")
        </li>
    }
</ul>
